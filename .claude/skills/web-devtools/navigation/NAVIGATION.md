# Navigation Guide

This is technical documentation for a command-line browser automation tool called browser-use. It covers navigation commands (open URLs, go back), scrolling options, tab management, and cookie operations (get, set, clear, export, import). The documentation also includes page information retrieval and three example workflows for common tasks like maintaining login sessions, multi-tab operations, and handling infinite scroll pages. A command reference table is provided at the end for quick lookup.
