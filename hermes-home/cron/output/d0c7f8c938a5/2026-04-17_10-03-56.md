# Cron Job: 光学前沿日报

**Job ID:** d0c7f8c938a5
**Run Time:** 2026-04-17 10:03:56
**Schedule:** 0 8 * * * (Asia/Shanghai)

## Prompt

[SYSTEM: The user has invoked the "web-search-guide" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: web-search-guide
description: 搜索工具使用规范 — MiniMax、Tavily、Semantic Scholar 的分工
---

# Web Search Guide — 搜索工具使用规范

## 工具选择原则

**通用流程：MiniMax 优先 → 质量不够再升级**

| 场景 | 首选用 | 升级备用 | 原因 |
|------|--------|----------|------|
| 新闻、天气、日常信息 | **MiniMax** | — | 速度快 (~0.76s) |
| 科技新闻 | **MiniMax** | **Tavily** | MiniMax 够用时先用 |
| 学术论文 | **MiniMax** 先试 | **Tavily** / **Semantic Scholar** | MiniMax 快速初筛，质量不够再上重型工具 |
| 专业知识、术语解释 | **MiniMax** | **Tavily** | 同上 |
| 技术文档、API | **MiniMax** | **Tavily** | 同上 |
| 精确论文检索 | — | **Semantic Scholar** | 学术专用 |

**决策逻辑**：
1. 优先用 MiniMax（快，够用）
2. 如果结果质量不高/不够权威，切换 Tavily
3. 论文类优先 Semantic Scholar，但 MiniMax 可作为快速预检

## MiniMax MCP

- **工具名**: `mcp__MiniMax__web_search`
- **配置**: `uvx minimax-coding-plan-mcp -y`，通过 MiniMax API 代理
- **响应时间**: ~0.76s
- **适用**: 快速日常搜索，不确定用哪个时的首选

## Tavily

- **工具名**: `WebSearch` (内置) 或 `tavily-search` (skill)
- **配置**: HTTP MCP (`https://mcp.tavily.com/mcp/`)
- **适用**: 需要准确、专业、深度内容时使用

## Semantic Scholar

- **工具名**: `mcp__semantic-scholar__*`
- **配置**: npx `@xbghc/semanticscholar-mcp`
- **适用**: 学术论文搜索

## 调用示例

```python
# MiniMax — 日常搜索
mcp__MiniMax__web_search(query="今天北京天气")

# Tavily — 技术/学术搜索
WebSearch(query="transformer attention mechanism paper 2024")

# Semantic Scholar — 论文搜索
mcp__semantic_scholar__search_papers(query="attosecond science review")
```

## 常见错误

- `Tool not found` → MCP 服务器未正确启动，先运行 `claude mcp list` 检查
- `Permission denied` → 检查 `permissions.allow` 是否包含 `MCP *`
- MiniMax 显示 ✗ Failed to connect → uvx 冷启动慢，运行一次 `claude mcp list` 预热即可

[SYSTEM: The user has invoked the "arxiv" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: arxiv
description: Search and retrieve academic papers from arXiv using their free REST API. No API key needed. Search by keyword, author, category, or ID. Combine with web_extract or the ocr-and-documents skill to read full paper content.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [Research, Arxiv, Papers, Academic, Science, API]
    related_skills: [ocr-and-documents]
---

# arXiv Research

Search and retrieve academic papers from arXiv via their free REST API. No API key, no dependencies — just curl.

## Quick Reference

| Action | Command |
|--------|---------|
| Search papers | `curl "https://export.arxiv.org/api/query?search_query=all:QUERY&max_results=5"` |
| Get specific paper | `curl "https://export.arxiv.org/api/query?id_list=2402.03300"` |
| Read abstract (web) | `web_extract(urls=["https://arxiv.org/abs/2402.03300"])` |
| Read full paper (PDF) | `web_extract(urls=["https://arxiv.org/pdf/2402.03300"])` |

## Searching Papers

The API returns Atom XML. Parse with `grep`/`sed` or pipe through `python3` for clean output.

### Basic search

```bash
curl -s "https://export.arxiv.org/api/query?search_query=all:GRPO+reinforcement+learning&max_results=5"
```

### Clean output (parse XML to readable format)

```bash
curl -s "https://export.arxiv.org/api/query?search_query=all:GRPO+reinforcement+learning&max_results=5&sortBy=submittedDate&sortOrder=descending" | python3 -c "
import sys, xml.etree.ElementTree as ET
ns = {'a': 'http://www.w3.org/2005/Atom'}
root = ET.parse(sys.stdin).getroot()
for i, entry in enumerate(root.findall('a:entry', ns)):
    title = entry.find('a:title', ns).text.strip().replace('\n', ' ')
    arxiv_id = entry.find('a:id', ns).text.strip().split('/abs/')[-1]
    published = entry.find('a:published', ns).text[:10]
    authors = ', '.join(a.find('a:name', ns).text for a in entry.findall('a:author', ns))
    summary = entry.find('a:summary', ns).text.strip()[:200]
    cats = ', '.join(c.get('term') for c in entry.findall('a:category', ns))
    print(f'{i+1}. [{arxiv_id}] {title}')
    print(f'   Authors: {authors}')
    print(f'   Published: {published} | Categories: {cats}')
    print(f'   Abstract: {summary}...')
    print(f'   PDF: https://arxiv.org/pdf/{arxiv_id}')
    print()
"
```

## Search Query Syntax

| Prefix | Searches | Example |
|--------|----------|---------|
| `all:` | All fields | `all:transformer+attention` |
| `ti:` | Title | `ti:large+language+models` |
| `au:` | Author | `au:vaswani` |
| `abs:` | Abstract | `abs:reinforcement+learning` |
| `cat:` | Category | `cat:cs.AI` |
| `co:` | Comment | `co:accepted+NeurIPS` |

### Boolean operators

```
# AND (default when using +)
search_query=all:transformer+attention

# OR
search_query=all:GPT+OR+all:BERT

# AND NOT
search_query=all:language+model+ANDNOT+all:vision

# Exact phrase
search_query=ti:"chain+of+thought"

# Combined
search_query=au:hinton+AND+cat:cs.LG
```

## Sort and Pagination

| Parameter | Options |
|-----------|---------|
| `sortBy` | `relevance`, `lastUpdatedDate`, `submittedDate` |
| `sortOrder` | `ascending`, `descending` |
| `start` | Result offset (0-based) |
| `max_results` | Number of results (default 10, max 30000) |

```bash
# Latest 10 papers in cs.AI
curl -s "https://export.arxiv.org/api/query?search_query=cat:cs.AI&sortBy=submittedDate&sortOrder=descending&max_results=10"
```

## Fetching Specific Papers

```bash
# By arXiv ID
curl -s "https://export.arxiv.org/api/query?id_list=2402.03300"

# Multiple papers
curl -s "https://export.arxiv.org/api/query?id_list=2402.03300,2401.12345,2403.00001"
```

## BibTeX Generation

After fetching metadata for a paper, generate a BibTeX entry:

{% raw %}
```bash
curl -s "https://export.arxiv.org/api/query?id_list=1706.03762" | python3 -c "
import sys, xml.etree.ElementTree as ET
ns = {'a': 'http://www.w3.org/2005/Atom', 'arxiv': 'http://arxiv.org/schemas/atom'}
root = ET.parse(sys.stdin).getroot()
entry = root.find('a:entry', ns)
if entry is None: sys.exit('Paper not found')
title = entry.find('a:title', ns).text.strip().replace('\n', ' ')
authors = ' and '.join(a.find('a:name', ns).text for a in entry.findall('a:author', ns))
year = entry.find('a:published', ns).text[:4]
raw_id = entry.find('a:id', ns).text.strip().split('/abs/')[-1]
cat = entry.find('arxiv:primary_category', ns)
primary = cat.get('term') if cat is not None else 'cs.LG'
last_name = entry.find('a:author', ns).find('a:name', ns).text.split()[-1]
print(f'@article{{{last_name}{year}_{raw_id.replace(\".\", \"\")},')
print(f'  title     = {{{title}}},')
print(f'  author    = {{{authors}}},')
print(f'  year      = {{{year}}},')
print(f'  eprint    = {{{raw_id}}},')
print(f'  archivePrefix = {{arXiv}},')
print(f'  primaryClass  = {{{primary}}},')
print(f'  url       = {{https://arxiv.org/abs/{raw_id}}}')
print('}')
"
```
{% endraw %}

## Reading Paper Content

After finding a paper, read it:

```
# Abstract page (fast, metadata + abstract)
web_extract(urls=["https://arxiv.org/abs/2402.03300"])

# Full paper (PDF → markdown via Firecrawl)
web_extract(urls=["https://arxiv.org/pdf/2402.03300"])
```

For local PDF processing, see the `ocr-and-documents` skill.

## Common Categories

| Category | Field |
|----------|-------|
| `cs.AI` | Artificial Intelligence |
| `cs.CL` | Computation and Language (NLP) |
| `cs.CV` | Computer Vision |
| `cs.LG` | Machine Learning |
| `cs.CR` | Cryptography and Security |
| `stat.ML` | Machine Learning (Statistics) |
| `math.OC` | Optimization and Control |
| `physics.comp-ph` | Computational Physics |

Full list: https://arxiv.org/category_taxonomy

## Helper Script

The `scripts/search_arxiv.py` script handles XML parsing and provides clean output:

```bash
python scripts/search_arxiv.py "GRPO reinforcement learning"
python scripts/search_arxiv.py "transformer attention" --max 10 --sort date
python scripts/search_arxiv.py --author "Yann LeCun" --max 5
python scripts/search_arxiv.py --category cs.AI --sort date
python scripts/search_arxiv.py --id 2402.03300
python scripts/search_arxiv.py --id 2402.03300,2401.12345
```

No dependencies — uses only Python stdlib.

---

## Semantic Scholar (Citations, Related Papers, Author Profiles)

arXiv doesn't provide citation data or recommendations. Use the **Semantic Scholar API** for that — free, no key needed for basic use (1 req/sec), returns JSON.

### Get paper details + citations

```bash
# By arXiv ID
curl -s "https://api.semanticscholar.org/graph/v1/paper/arXiv:2402.03300?fields=title,authors,citationCount,referenceCount,influentialCitationCount,year,abstract" | python3 -m json.tool

# By Semantic Scholar paper ID or DOI
curl -s "https://api.semanticscholar.org/graph/v1/paper/DOI:10.1234/example?fields=title,citationCount"
```

### Get citations OF a paper (who cited it)

```bash
curl -s "https://api.semanticscholar.org/graph/v1/paper/arXiv:2402.03300/citations?fields=title,authors,year,citationCount&limit=10" | python3 -m json.tool
```

### Get references FROM a paper (what it cites)

```bash
curl -s "https://api.semanticscholar.org/graph/v1/paper/arXiv:2402.03300/references?fields=title,authors,year,citationCount&limit=10" | python3 -m json.tool
```

### Search papers (alternative to arXiv search, returns JSON)

```bash
curl -s "https://api.semanticscholar.org/graph/v1/paper/search?query=GRPO+reinforcement+learning&limit=5&fields=title,authors,year,citationCount,externalIds" | python3 -m json.tool
```

### Get paper recommendations

```bash
curl -s -X POST "https://api.semanticscholar.org/recommendations/v1/papers/" \
  -H "Content-Type: application/json" \
  -d '{"positivePaperIds": ["arXiv:2402.03300"], "negativePaperIds": []}' | python3 -m json.tool
```

### Author profile

```bash
curl -s "https://api.semanticscholar.org/graph/v1/author/search?query=Yann+LeCun&fields=name,hIndex,citationCount,paperCount" | python3 -m json.tool
```

### Useful Semantic Scholar fields

`title`, `authors`, `year`, `abstract`, `citationCount`, `referenceCount`, `influentialCitationCount`, `isOpenAccess`, `openAccessPdf`, `fieldsOfStudy`, `publicationVenue`, `externalIds` (contains arXiv ID, DOI, etc.)

---

## Complete Research Workflow

1. **Discover**: `python scripts/search_arxiv.py "your topic" --sort date --max 10`
2. **Assess impact**: `curl -s "https://api.semanticscholar.org/graph/v1/paper/arXiv:ID?fields=citationCount,influentialCitationCount"`
3. **Read abstract**: `web_extract(urls=["https://arxiv.org/abs/ID"])`
4. **Read full paper**: `web_extract(urls=["https://arxiv.org/pdf/ID"])`
5. **Find related work**: `curl -s "https://api.semanticscholar.org/graph/v1/paper/arXiv:ID/references?fields=title,citationCount&limit=20"`
6. **Get recommendations**: POST to Semantic Scholar recommendations endpoint
7. **Track authors**: `curl -s "https://api.semanticscholar.org/graph/v1/author/search?query=NAME"`

## Rate Limits

| API | Rate | Auth |
|-----|------|------|
| arXiv | ~1 req / 3 seconds | None needed |
| Semantic Scholar | 1 req / second | None (100/sec with API key) |

## Notes

- arXiv returns Atom XML — use the helper script or parsing snippet for clean output
- Semantic Scholar returns JSON — pipe through `python3 -m json.tool` for readability
- arXiv IDs: old format (`hep-th/0601001`) vs new (`2402.03300`)
- PDF: `https://arxiv.org/pdf/{id}` — Abstract: `https://arxiv.org/abs/{id}`
- HTML (when available): `https://arxiv.org/html/{id}`
- For local PDF processing, see the `ocr-and-documents` skill

## ID Versioning

- `arxiv.org/abs/1706.03762` always resolves to the **latest** version
- `arxiv.org/abs/1706.03762v1` points to a **specific** immutable version
- When generating citations, preserve the version suffix you actually read to prevent citation drift (a later version may substantially change content)
- The API `<id>` field returns the versioned URL (e.g., `http://arxiv.org/abs/1706.03762v7`)

## Withdrawn Papers

Papers can be withdrawn after submission. When this happens:
- The `<summary>` field contains a withdrawal notice (look for "withdrawn" or "retracted")
- Metadata fields may be incomplete
- Always check the summary before treating a result as a valid paper

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是浙江大学光学博士的学术助手，负责追踪超快光学及其相关前沿领域的最新学术动态。

## 任务

分 4 个子方向检索过去 3 天内的最新学术动态，每个方向选取最值得关注的 1-2 条，总共精选 4-6 条，整理成精读简报。

### 检索方向（4 个）
1. **超快光学 (Ultrafast Optics)** — 阿秒脉冲、高次谐波、飞秒激光、THz 辐射、超快光谱
2. **光芯片 (Photonic Chips)** — 硅光集成、InP 光子芯片、CMOS 光电集成
3. **光计算 (Optical Computing)** — 光学神经网络、全光计算、光电混合计算、光学推理加速
4. **超表面 (Metasurfaces)** — 介质超表面、可调超表面、动态全息、超透镜

### 检索策略（按优先级）
1. **arXiv** — 搜索 physics.optics 和 cond-mat.mes-hall 分类
2. **MiniMax web_search** — `mcp__MiniMax__web_search` 搜索各方向最新新闻和论文
3. **Semantic Scholar** — `mcp__semantic_scholar__*` 补充检索重要论文

关键词：
- ultrafast laser OR attosecond OR high-harmonic OR femtosecond
- silicon photonics OR photonic integrated circuit
- optical neural network OR all-optical computing OR photonic computing
- metasurface OR metalens OR dielectric metasurface

### 输出格式（中文，飞书推送）

```
🌟 光学前沿日报 | {日期}

━━━━━━━━━━━━━━━
📌 超快光学
[标题] [来源] [日期]
▸ 关键进展：2-3 句话概括核心突破
▸ 点评：为什么重要
🔗 链接

━━━━━━━━━━━━━━━
📌 光芯片 / 光计算 / 超表面（如无重要新闻可跳过）
...
```

### 质量标准
- 优先选：Nature/Science/Cell 子刊、Optica、PRX、Advanced Photonics、arXiv
- 每条必须有实质性科学进展
- 总字数 800-1200 字，宁缺毋滥

### 推送目标
飞书 Home (oc_4c05763dd5c2edc78a09eee8b452c2a8)

## Response

🌟 光学前沿日报 | 2026-04-17

━━━━━━━━━━━━━━━
📌 超快光学

**[Roadmap on Attosecond Science](https://arxiv.org/abs/2604.15085) — arXiv · 2026-04-16**
▸ 关键进展：包含 Pierre Agostini（诺贝尔奖得主）在内的近百位全球顶尖阿秒科学专家联合发布了阿秒物理领域首份系统性路线图。报告覆盖过去25年阿秒脉冲产生与表征技术的演进、新型激光波形合成、高次谐波新方案，以及阿秒技术在原子分子固体中电子动力学探测与操控的前沿应用，并对下一代光源与测量技术做出前瞻性展望。
▸ 点评：这是阿秒领域里程碑式的综述文献，汇集了从基础理论到工程实现的完整图景，对把握该领域全局脉络、把握未来5-10年发展方向具有重要参考价值，是必读之作。
🔗 https://arxiv.org/abs/2604.15085

**[Contrasting ultrafast dynamics in monolayer MoS₂ and NbSe₂ via HHG](https://arxiv.org/abs/2604.13901) — arXiv · 2026-04-15**
▸ 关键进展：研究通过求解含时含电子-电子相互作用的多带约化密度矩阵方程，系统比较了单层 MoS₂ 与金属性 NbSe₂ 中的强场驱动高次谐波产生（HHG）动力学。发现在 MoS₂ 中电子-电子相互作用显著增强谐波产率并改变相位与角分布，而 NbSe₂ 中增强较弱但仍有明显的相位与角分辨信号差异，并将差异归因于材料不同的光学共振与能带结构。
▸ 点评：该工作深化了对二维材料中 HHG 物理机制的理解，特别是电子-电子相互作用在强场非线性过程中的角色，为利用 HHG 表征二维材料电子结构提供了理论框架。
🔗 https://arxiv.org/abs/2604.13901

━━━━━━━━━━━━━━━
📌 光芯片

**[Low voltage and high-bandwidth thin-film lithium tantalate modulator](https://arxiv.org/abs/2604.14836) — arXiv · 2026-04-16**
▸ 关键进展：研究展示了基于薄膜钽酸锂（Lithium Tantalate, LT）平台的集成电光调制器。与已被广泛研究的薄膜铌酸锂（LNOI）相比，钽酸锂具有更低的直流漂移、更高的光功率承受能力及更低的双折射，且该工作实现了低驱动电压与高带宽的兼得，为下一代密集光子集成电路提供了更优的材料平台。
▸ 点评：钽酸锂作为商业化已成熟的滤波材料，其在集成光子学领域的潜力长期被低估。该工作填补了薄膜钽酸锂调制器性能数据的空白，有望推动数据中心和光通信调制器向更低价、更稳定的方向发展。
🔗 https://arxiv.org/abs/2604.14836

**[Ultrawide-angle 2D beam steering via hybrid integrated metasurface-photonic circuit](https://arxiv.org/abs/2604.13233) — arXiv · 2026-04-14**
▸ 关键进展：MIT 团队展示了一种芯片级超宽角二维衍射极限光束扫描平台，将硅光子集成电路（PIC）与光学超表面进行混合集成。通过自由曲面微光学反射器配合片上光栅耦合器，实现了方位角与仰角两维度的宽视场快速扫描，可支持卫星间光链、机载激光雷达及自由空间光通信等应用。
▸ 点评：光束扫描是集成光学的长期难题，该工作巧妙利用超表面弥补了纯PIC器件在角度覆盖和光束质量上的不足，是超表面与光子芯片异质集成的重要里程碑，对未来集成相控阵和光通信系统具有实用价值。
🔗 https://arxiv.org/abs/2604.13233

━━━━━━━━━━━━━━━
📌 光计算

**[Scaling Photonic Tensor Cores with Unary and Homodyne Designs](https://arxiv.org/abs/2604.14664) — arXiv · 2026-04-16**
▸ 关键进展：研究系统分析了五种基于微环的光子张量核架构（光子计算芯片核心单元），建立了统一的功耗模型。关键发现是：电路排序、一元编码（unary encoding）和外差累积（homodyne accumulation）是决定扩展性的三大要素，其中后两者为提升并行度提供了最有效路径，为大规模光子计算系统的工程化落地提供了量化设计准则。
▸ 点评：光子计算近年来从概念验证逐步走向可扩展系统设计，该工作以统一框架对比分析了实际硬件层面的瓶颈，对研发高算力光子 AI 加速芯片具有直接指导意义。
🔗 https://arxiv.org/abs/2604.14664

━━━━━━━━━━━━━━━
📌 超表面

**[End-to-End Inverse Designed Metasurfaces for Snapshot RGB-Achromatic Full-Stokes Polarization Imaging](https://arxiv.org/abs/2604.14901) — arXiv · 2026-04-16**
▸ 关键进展：研究提出端到端联合优化框架，将可微分4f光学前端（含超表面编码器）与 U-Net 神经网络后端协同设计，实现了从单帧单色传感器输出同时重建 RGB 三通道全斯托克斯偏振图像（包含线偏振、圆偏振信息）。其中超表面由多层感知器（MLP）建模实现可微分化设计，系统在真实数据集上达到 30 dB PSNR 和 0.83 SSIM。
▸ 点评：该工作将超表面的物理建模与深度学习端到端联合优化，开辟了"计算超表面"新范式——超表面本身不再仅是静态光学元件，而是可学习、可逆设计的智能光学前端，在紧凑型偏振相机、AR/VR 光学等方面应用前景广阔。
🔗 https://arxiv.org/abs/2604.14901

━━━━━━━━━━━━━━━
📝 本日小结
本期五条精读文献均来自 arXiv（2026-04-14~16），涵盖阿秒科学、二维材料强场物理、集成光子调制、光子计算扩展性及计算超表面五个方向。最大亮点是阿秒路线图（Agostini 等）和端到端超表面偏振成像——前者是领域共识性文献，后者代表超表面从被动整形向智能光学系统演进的新趋势。钽酸锂调制器和混合集成光束扫描则体现了集成光子学向实用化演进的最新进展。
